services:
  - type: web
    name: your-app-name
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py   # or gunicorn main:app, etc.
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
